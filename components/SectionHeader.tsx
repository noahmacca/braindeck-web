
const SectionHeader = ({text}: {text: string}) => (
    <div className="md:text-center text-4xl mb-2 mt-6 md:mb-6 md:mt-12 tracking-tight font-light text-gray-700 capitalize">
        {text}
    </div>
)

export default SectionHeader;